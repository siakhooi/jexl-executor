#!/bin/sh
set -e

RELEASE_VERSION=1.2.0
RELEASE_TITLE="$RELEASE_VERSION"
RELEASE_NOTE='- support json
- new option: --full, -F to print full context instead of result
- debug log with json format'
ISSUE_TO_CLOSE=""

export RELEASE_VERSION RELEASE_TITLE RELEASE_NOTE ISSUE_TO_CLOSE
