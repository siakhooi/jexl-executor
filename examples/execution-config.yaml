resultPathTemplate: "{name}"
flows:
  default:
    contextFile: context.json
    scriptFiles:
      - script2.jexl
    exitCodeExpr: "@file:exit.jexl"
  flow3:
    contextFile: context.json
    scriptFiles:
      - script3.jexl
